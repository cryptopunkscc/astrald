# AI Workspace Bridge

This file only bridges agents to the core AI workspace in `.ai/`.

Start with `.ai/README.md`, then follow its load order and indexes.

Do not duplicate project knowledge here. Update `.ai/` instead.
