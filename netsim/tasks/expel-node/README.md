# expel-node

The User (node1) permanently bans node2 from the swarm via `user.expel`. `verify.py` proves node2 is in `user.list_expelled` and gone from `user.swarm_status`. From `two-nodes`; produces `two-nodes-expel`.
