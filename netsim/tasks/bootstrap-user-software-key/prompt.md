Make this node a User node with a new software key — install its active user contract, not just create the key.

Save the user's id and an access token to `~/user.json` (as `user_id` and
`user_token`).
